-- All the 3 files(html,css,jpeg) should always be in one folder. Because, the paths for referncing the style sheet and image is made with keeping the files in the same folder.
-- If some files are in different place, the the corresponding content would not be loaded.
-- After having all the files in 1 folder, then, open on the HTML file, it will open in your default browser.

--Main Aspects of this project
  -- The blur effect of background image.
  -- The blur effect is done by CSS.
  -- SIMPLE and CLEAN interface
